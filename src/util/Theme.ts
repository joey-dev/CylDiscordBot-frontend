import { createTheme } from '@mui/material/styles';


const theme = createTheme({
    palette: {
        mode: 'dark',
        primary: {
            main: '#36393f',
        },
        secondary: {
            main: '#ffffff',
        },
        info: {
            main: '#e2409e',
        },
    },
});

export default theme;
